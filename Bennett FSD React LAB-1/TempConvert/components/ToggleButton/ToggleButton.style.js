import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    btn : {
        backgroundColor: "red",
        padding: 10,
        width: '60%',
        textAlign: "center",
    },
    btnText: {
        alignSelf: "center",
        fontWeight: "bold",
        color: "#fff",
        fontSize: 20
    }
});