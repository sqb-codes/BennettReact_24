import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        backgroundColor: "#fff",
        padding: 20,
        borderRadius: 30,
        width: 300
    },
    buttonTxt: {
        color: "#000",
        fontSize: 20,
        textAlign: "center"
    }
})