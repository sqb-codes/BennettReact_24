import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputWrapper: {
        width: '80%'
    },
    textInput: {
        backgroundColor: "#fff",
        padding: 10,
        height: 60,
        fontSize: 20
    }
});